<nav class="navbar navbar-expand-lg navbar-light bg-light sticky-top">
    <div class="container">
     <a class="navbar-brand" data-aos="flip-left" data-aos-duration="1000" href="#"> <img src="/images/diary-icon-46554.png" width="40px" alt=""> Diary of Dreams</a>
     <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" >
       <span class="navbar-toggler-icon"></span>
     </button>
 
     <div class="collapse navbar-collapse" id="navbarNav">
       <ul class="navbar-nav mx-auto">   
        <li class="nav-item">
          <a class="nav-link mr-3 ml-5 <%= path =='explore' ? 'active' : '' %>" href="/explore">Explore</a>
        </li>
        <% if (isLoggedIn) { %>

          <% if (user.profile) { %>
            <li class="nav-item">
              <a class="nav-link mr-3 <%= path =='profile' ? 'active' : '' %>" href="/dashboard/myProfile">Profile</a>
            </li>
            <li class="nav-item">
              <a class="nav-link mr-3 <%= path =='home' ? 'active' : '' %>" href="/explore/home">Home</a>
            </li>
          <% } %>

          <% if (!user.profile) { %>
            <li class="nav-item">
              <a class="nav-link mr-3 <%= path =='profile' ? 'active' : '' %>" href="/dashboard/createProfile">Create Profile</a>
            </li>
          <% } %>

          <li class="nav-item">
            <a class="nav-link mr-3" href="/auth/logout">Logout</a>
          </li>
          
        <% }else{ %>
          <li class="nav-item">
            <a class="nav-link mr-3 <%= path=='signpath' ? 'active' : '' %>  " href="/auth/signup">Sign Up</a>
          </li>
          <li class="nav-item <%= path=='logpath' ? 'active' : '' %> ">
            <a class="nav-link mr-3" href="/auth/login">Login</a>
          </li>

          <% } %>
          
         
       </ul>
 
      <form action="/search" method="GET" class="form-inline">
       <input type="search" name="term" class="form-control search-bar" placeholder="Search Here" required>
       <button type="submit" class="btn btn-warning search-btn">Search</button>
      </form>
 
     </div>
    </div>
   </nav>